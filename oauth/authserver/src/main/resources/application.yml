spring:
  application:
    name: authserver

logging:
  level:
    org:
      springframework:
        security: trace

  security:
    oauth2:
      authorizationserver:
        client:
          client-1:
            registration:
              client-id: "client"
              client-secret: "{bcrypt}$2a$10$FJeuh2H3CqZKljRi1s5pQOPBHsOOw6h8x3D7fS56wOOE4/fsX3ocG"
              client-authentication-methods:
                - "client_secret_basic"
              authorization-grant-types:
                - "client_credentials"
                - "authorization_code"
                - "refresh_token"
              redirect-uris:
                - "http://127.0.0.1:8082/login/oauth2/code/spring"
              scopes:
                - "user.read"
                - "user.write"
                - "openid"
              require-authorization-consent: true
